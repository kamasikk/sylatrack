# Public launch on Render

1. Upload the project code to GitHub.
2. In Render select New, then Blueprint, then choose the repository.
3. Render reads render.yaml. Apply the plan.
4. Open the resulting https address.

HTTPS is enabled by Render. For permanent shared production data, connect an external database such as Supabase or Postgres. The local JSON file is only for local development.
